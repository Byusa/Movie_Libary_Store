from django.urls import path

from . import views

urlpatterns = [
    # path("", views.get_all_recommendations),
    # path("<int:recommendation_id>/", views.get_recommendation),
    # path("create/", views.create_recommendation),
    # path("update/<int:recommendation_id>/", views.update_recommendation),
    # path("delete/<int:recommendation_id>/", views.delete_recommendation),
]
