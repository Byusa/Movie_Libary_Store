from django.urls import path

from . import views

urlpatterns = [
    # path("", views.get_all_media),
    # path("<int:media_id>/", views.get_media),
    # path("create/", views.create_media),
    # path("update/<int:media_id>/", views.update_media),
    # path("delete/<int:media_id>/", views.delete_media),
]
