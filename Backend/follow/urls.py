from django.urls import path

from . import views

urlpatterns = [
    # path("", views.get_all_follows),
    # path("<int:follow_id>/", views.get_follow),
    # path("create/", views.create_follow),
    # path("update/<int:follow_id>/", views.update_follow),
    # path("delete/<int:follow_id>/", views.delete_follow),
]
