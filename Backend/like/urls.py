from django.urls import path

from . import views

urlpatterns = [
    # path("", views.get_all_likes),
    # path("<int:like_id>/", views.get_like),
    # path("create/", views.create_like),
    # path("update/<int:like_id>/", views.update_like),
    # path("delete/<int:like_id>/", views.delete_like),
]
