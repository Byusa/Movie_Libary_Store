from django.urls import path

from . import views

urlpatterns = [
    # path("", views.get_all_chats),
    # path("<int:chat_id>/", views.get_chat),
    # path("create/", views.create_chat),
    # path("update/<int:chat_id>/", views.update_chat),
    # path("delete/<int:chat_id>/", views.delete_chat),
]
